<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- v1: sync via HTTP/HTTPS (self-hosted IP diizinkan, warning di UI) + SSH -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Background survival: foreground-service anchor (specialUse: keep SSH
         sessions alive while backgrounded) + optional partial wake lock.
         POST_NOTIFICATIONS (API 33+) is requested once on first connect:
         the FGS notification itself is exempt, but without the grant the
         app-level switch cannot be managed from settings (FGS notice sinks
         with it) and the one-shot "session lost" notice can never show. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="izs SSH"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Session anchor: started when the first session connects, stopped
             when the last one goes away. specialUse + subtype property are
             required on API 34+ (targetSdk 36); older releases ignore them.
             Sideloaded distribution, so no Play review applies. -->
        <service
            android:name=".core.session.SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep SSH terminal sessions alive while the app is in the background." />
        </service>
    </application>
</manifest>
